# Verified against these exact versions (Python 3.11).
numpy==2.4.4
pandas==3.0.2
pyarrow==25.0.1        # parquet reader for traffic_train / traffic_validation
lightgbm==4.6.0
matplotlib==3.10.9     # viz/*.py only
